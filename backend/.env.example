# Configuração do Backend - PenseOffline

# ===========================================
# BANCO DE DADOS
# ===========================================

# Para desenvolvimento local (SQLite):
# DATABASE_URL=sqlite:///./app.db

# Para produção com Supabase:
# DATABASE_URL=postgresql://postgres:SUA_SENHA@db.PROJECT_ID.supabase.co:5432/postgres

# Para produção com PostgreSQL local:
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/penseoffline

DATABASE_URL=sqlite:///./app.db

# ===========================================
# CORS (Cross-Origin Resource Sharing)
# ===========================================

# Lista de origens permitidas para requisições (separadas por vírgula)
# Necessário para permitir que o frontend no Vercel acesse o backend
# 
# IMPORTANTE: OBRIGATÓRIO em produção (ENVIRONMENT=production)
# O backend não iniciará sem esta variável em modo produção por segurança
# 
# Exemplos:
# CORS_ORIGINS=https://seu-app.vercel.app
# CORS_ORIGINS=https://seu-app.vercel.app,https://www.seu-dominio.com
#
# Para desenvolvimento local, deixe vazio ou não defina (usa localhost automaticamente)

# CORS_ORIGINS=https://seu-app.vercel.app

# ===========================================
# AMBIENTE
# ===========================================

# Define o ambiente de execução
# Valores: development, production
# IMPORTANTE: Em modo production, CORS_ORIGINS deve estar definido

# ENVIRONMENT=production

# ===========================================
# SEGURANÇA
# ===========================================

# Chave secreta para assinar tokens JWT
# IMPORTANTE: Gere uma chave aleatória forte para produção!
# Exemplo de comando para gerar: openssl rand -hex 32
# Mínimo 32 caracteres recomendado

# SECRET_KEY=sua-chave-secreta-aleatoria-aqui-min-32-caracteres

# ===========================================
# EMAIL (Opcional)
# ===========================================

# Configurações para envio de emails (opcional)
# Se não configurado, emails serão apenas logados no console

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu-email@gmail.com
# SMTP_PASSWORD=sua-senha-app
# FROM_EMAIL=noreply@penseoffline.com
# FROM_NAME=Pense Offline

# ===========================================
# OUTRAS CONFIGURAÇÕES
# ===========================================

# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Porta do servidor (padrão: 8000)
# PORT=8000
