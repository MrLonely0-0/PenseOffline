name: Deploy backend + apply DB (DESABILITADO - usando Vercel)

on:
  workflow_dispatch:

jobs:
  deploy-and-db:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow desabilitado
        run: |
          echo "Este workflow foi desabilitado."
          echo "Deploy automático agora funciona via Vercel + vercel.json"
          echo "Para aplicar migrations, use o Supabase Dashboard ou execute manualmente:"
          echo "  1. Acesse https://app.supabase.com"
          echo "  2. SQL Editor > New Query"
          echo "  3. Cole backend/schema_postgres.sql e execute"
          echo "  4. Cole backend/seed_postgres.sql e execute"
          exit 0
